SOLANA_RPC_URL=https://api.devnet.solana.com
# Generate a new keypair with: solana-keygen new --outfile ~/.config/solana/id.json
# Then export it as base58: solana config get keypair && cat ~/.config/solana/id.json
SOLANA_KEYPAIR=